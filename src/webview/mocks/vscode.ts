/**
 * Minimal vscode module stub for Storybook.
 * The webview code never imports 'vscode' directly, but this alias
 * exists as a safety net in case any transitive import tries to resolve it.
 */
export default {};
